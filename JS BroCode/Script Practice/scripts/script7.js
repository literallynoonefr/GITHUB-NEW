/*
    MATH

    Built-in object that provides a collection of properties
    and methods
*/

// console.log(Math.PI);
// console.log(Math.E); //Eulers


////let x = -3.21;
//let x = 45;
//let x = 10;
//let x = 81;
//let x = 3;
//let x = 3.21;
//let x = 3.99;
////let y = 2;
////let z;

//z = Math.round(x); //round provides a round number

//z = Math.floor(x); //floor rounds the number DOWN

//z = Math.ceil(x); //ceil rounds the number UP

//z = Math.trunc(x); //truncate eliminates any decimal portion

//z = Math.pow(x, y); //pow = exponentiation
                    // x to the power of y

//z = Math.sqrt(x); //square root

//z = Math.log(x); //logarithm


/* 
    TRIGONOMETRY
*/

//z = Math.sin(x); //seno
//z = Math.cos(x); //cosseno
//z = Math.tan(x); //tangente


/* 
    FIND THE ABSOLUTE VALUE OF A NUMBER
    (é o mesmo número, só que sempre positivo +)
*/

//z = Math.abs(x)


/*
    FIND THE SIGN (sinal + ou -) of a number

    Retorna 1 se for positivo
    Retorna -1 se for negativo
    Retorna 0 se for 0 positivo
    Retorna -0 se for 0 negativo
*/

//z = Math.sign(x)



/*
    FIND THE MAXIMUM or MINIMUM value between variables
*/

let x = 3;
let y = 2;
let z = 1;
//let max = Math.max(x, y, z); //finds the maximum
//let min = Math.min(x, y, z);

console.log(min);